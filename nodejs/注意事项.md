# 1.在 vscode 中如何运行 js 文件：

在资源管理器的文件右键-在集成终端中打开-运行命令：`node .\例. js` 

# 2.reponse 解决页面乱码
`response.setHeader('content-type','text/html;charset=utf-8');`
# 3. 变量转换字符串

String (变量)
例：`if(String(method) == 'GET')`

# html 中静态资源的相对路径与绝对路径

**相对路径**
![[Pasted image 20230923154120.png]]
**绝对路径**
![[Pasted image 20230923154247.png]]
